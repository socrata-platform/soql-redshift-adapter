version:
  version: 5.2
redshift:
quarkus:
  log:
    level: DEBUG
  datasource:
    meta:
      db-kind: h2
      username: meta
      jdbc:
        url: jdbc:h2:mem:meta
        max-size: 11
    store:
      db-kind: h2
      username: store
      jdbc:
        url: jdbc:h2:mem:store
        max-size: 11
